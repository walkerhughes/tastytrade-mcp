# Task: Watchlist Symbols

List the ticker symbols in my watchlist named "My Tech".

Use the available Tastytrade MCP tools. When you have the answer, write a JSON object to
`/app/answer.json` with exactly this shape:

```json
{"symbols": ["...", "..."]}
```

Write only that file.
