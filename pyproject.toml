[project]
name = "tastytrade-mcp"
version = "0.1.0"
description = "MCP server exposing Tastytrade SDK as tools for Claude Code"
readme = "README.md"
requires-python = ">=3.13"
dependencies = [
    "tastytrade-sdk",
    "mcp[cli]",
]

[project.scripts]
tastytrade-mcp = "src.server:main"

[dependency-groups]
dev = [
    "pytest>=8.0",
    "ruff>=0.9",
    "mypy>=1.0",
    "pytest-cov>=6.0",
    "pytest-mock>=3.0",
    "pytest-xdist>=3.0",
    "python-dotenv>=1.0",
]

[tool.pytest.ini_options]
testpaths = ["tests"]
markers = [
    "unit: Unit tests (deselect with '-m \"not unit\"')",
    "integration: Integration tests (deselect with '-m \"not integration\"')",
]
addopts = "-v"

[tool.ruff]
line-length = 120
target-version = "py313"

[tool.ruff.lint]
select = ["E", "F", "I", "W"]

[tool.mypy]
ignore_missing_imports = true

[tool.coverage.run]
source = ["src"]
omit = ["tests/*"]

[tool.coverage.report]
fail_under = 80
show_missing = true

[tool.hatch.build.targets.wheel]
only-include = ["src"]
sources = { "src" = "" }

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"
