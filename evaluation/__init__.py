"""Evaluation for the Tastytrade MCP server.

`run.py` and `cases/` hold the LLM eval harness. `harbor/` holds the Harbor benchmark that
compares the v1 and v2 servers head to head. The fast deterministic misuse checks live with
the unit tests in `tests/unit/test_misuse_evals.py`.
"""
