[task]
name = "transaction-fee-total"
description = "Find the total fees across all of my transactions, in dollars."

[metadata]
suite = "tastytrade-mcp"

[environment]
docker_image = "tastytrade-bench"
network_mode = "public"

[agent]
timeout_sec = 300

[verifier]
timeout_sec = 60
