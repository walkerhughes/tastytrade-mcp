#!/usr/bin/env bash
# Launch the candidate (mcp-server-refactor) server over stdio against the mock API.
set -euo pipefail
require-local-api
start-mock
exec uv run --project /opt/mcp-candidate python -m src.server
