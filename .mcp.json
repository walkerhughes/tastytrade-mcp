{
  "mcpServers": {
    "tastytrade": {
      "type": "stdio",
      "command": "bash",
      "args": [
        "-c",
        "set -a && source .env && set +a && exec uv run python -m src.server"
      ],
      "env": {}
    }
  }
}